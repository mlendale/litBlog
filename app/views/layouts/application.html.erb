<!DOCTYPE html>
<html>
  <head>
    <title><%=full_title(yield(:title))%></title>
    

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  	<!--Could be put in a partial _header.html.erb-->
  	<header class="navbar navbar-fixed-top navbarlight bg-inverse">
		<div class="container">
	    	<nav>
	    		<ul class="nav navbar-nav">
	   				<% if logged_in?%>
    					<li class="nav-item active"><%= link_to "Post", root_url %></li>
    					<li class="nav-item"><%= link_to "See your posts", user_url(@current_user.name) %></li>
    					<li class="nav-item"><%= link_to "Edit your info", user_edit_url(@current_user.name) %></li>
      					<li class="nav-item"><%= link_to "Log out", logout_path,method:'delete' %></li>
      				<%else%>
      					<li class="nav-item active"><%= link_to "Home", root_path %></li>
      					<li class="nav-item"><%= link_to "Log in", login_path %></li>
    				<%end%>
	      		</ul>
	    	</nav>
		</div>
	</header>
	<div class="container">
		<% flash.each do |message_type, message| %>
			<div class="alert alert-<%= message_type %>"><%= message %></div>
		<% end %>

    	<%= yield %>
    </div>
  </body>
</html>
